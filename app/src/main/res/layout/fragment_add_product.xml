<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Контейнер для фото -->

    <!-- Описание / название -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/photoContainer"
        android:layout_width="0dp"
        android:layout_height="180dp"
        android:layout_marginTop="32dp"
        android:background="#DDDDDD"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Фото (скрыто по умолчанию) -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone"
            tools:src="@drawable/home" />

        <!-- Текст-заглушка -->
        <TextView
            android:id="@+id/textAddPhoto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Добавить фото"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <EditText
        android:id="@+id/descriptionProduct"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:hint="Описание товара"
        android:inputType="textMultiLine"
        app:layout_constraintTop_toBottomOf="@id/photoContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="96dp"/>

    <ImageView
        android:id="@+id/PhotoCategor"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="12dp"
        app:layout_constraintBottom_toTopOf="@id/descriptionProduct"
        app:layout_constraintEnd_toStartOf="@id/add_categories"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/photoContainer"
        app:layout_constraintVertical_bias="0.512" />

    <TextView
        android:id="@+id/add_categories"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="выберите категорию"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/photoContainer"
        app:layout_constraintBottom_toTopOf="@id/descriptionProduct"
        app:layout_constraintStart_toEndOf="@id/PhotoCategor"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginStart="12dp"/>

    <Button
        android:id="@+id/save_addProducts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="116dp"
        android:text="добавить объявление"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/descriptionProduct" />

    <EditText
        android:id="@+id/nameProduct"
        android:layout_width="115dp"
        android:layout_height="46dp"
        android:layout_marginTop="8dp"
        android:ems="10"

        android:hint="название"
        android:inputType="textMultiLine"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/photoContainer" />

    <EditText
        android:id="@+id/priceProduct"
        android:layout_width="77dp"
        android:layout_height="41dp"
        android:layout_marginBottom="64dp"
        android:ems="10"
        android:inputType="number"
        android:hint="цена"
        android:textSize="25dp"
        app:layout_constraintBottom_toTopOf="@+id/save_addProducts"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.079"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/quantityProduct"
        android:layout_width="114dp"
        android:layout_height="44dp"
        android:layout_marginStart="64dp"
        android:layout_marginBottom="64dp"
        android:ems="10"
        android:inputType="number"
        android:hint="кол.во"
        app:layout_constraintBottom_toTopOf="@+id/save_addProducts"
        app:layout_constraintStart_toEndOf="@+id/priceProduct" />


</androidx.constraintlayout.widget.ConstraintLayout>